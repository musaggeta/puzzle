name: puzzle
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  collection: ^1.17.2
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
